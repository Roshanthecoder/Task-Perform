<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DorbbyTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DorbbyTask" parent="Base.Theme.DorbbyTask">
        <item name="android:statusBarColor">@android:color/black</item>

    </style>

    <style name="ProgressBarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@android:color/holo_blue_dark</item> <!-- Change the colorAccent to your preferred color -->
    </style>
    <style name="ImageViewShapeAppearance">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item> <!-- Adjust the cornerSize to change the roundness -->
    </style>


</resources>